/**
 * Model-agnostic `LlmClient` interface and request/response models.
 *
 * Populated in phase-04. The concrete Claude implementation lives in
 * `com.assist.llm.anthropic`. Do NOT reference Anthropic SDK types outside that
 * sub-package. See `.claude/phases/phase-04-llm-claude.md`.
 */
package com.assist.llm
