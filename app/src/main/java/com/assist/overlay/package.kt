/**
 * Foreground service + Compose overlay (`SYSTEM_ALERT_WINDOW`) rendering the
 * live agent<->user exchange, interruptible.
 *
 * Populated in phase-07. See `.claude/phases/phase-07-overlay.md`.
 */
package com.assist.overlay
